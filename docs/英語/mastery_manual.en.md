# Mastery Manual (Simple EN)

- Validate `last write time`.
- `user!!` means manual edit.
- Warning only if `user!!` timestamp is far.
- Sync removes `user!!`.
- Reformat file when format/version is invalid.

See `LICENSE.md`.
